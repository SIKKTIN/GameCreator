# 项目启动与 AI 协作入口

“项目管理”下依次提供项目启动、工程连接、人员分配、项目排期、任务清单。工程连接负责保存引擎类型和实际工程根目录；“数据与同步 → 工程同步”负责同步配置、待同步变更、开发反馈和同步记录。

项目启动准备协作文件，不启动引擎进程，也不自动创建游戏场景。AI 以引擎工程为主要工作目录，通过实际运行验证设计；GameCreator 项目保存正式设计、分工、排期与验收记录。

## 准备与初始化

1. 将 GameCreator 项目保存到独立文件夹。
2. 在“工程连接”选择引擎并保存工程根目录。切换页面会保留未保存的连接草稿，保存前禁止初始化或工程同步。
3. 在“人员分配”创建开发者及长期令牌。至少一位启用的制作人需要制作管理岗位、项目范围、`project_write` 和内容模块授权。制作人可以没有具体任务。
4. 所有启用的开发者都需要启用的岗位和一个有效长期令牌。本机必须能从凭证保管库读取私钥；旧令牌仅有公开信息时，先导入已保存凭证或更换令牌。初始化不自动生成或更换身份。
5. 在“项目启动”设置协作入口子目录，默认 `gamecreator`；设置设计文档子目录，默认 `docs/gamecreator`。二者都相对于保存的引擎根目录，不能越界或与素材等输出目录重叠。
6. 预览初始化，检查公开文档的新增、更新、冲突和待移除内容。每项冲突必须明确保留或替换；删除旧路径另需勾选。确认后初始化。

如工程清单属于其他 GameCreator 项目，先到“工程同步”核对归属并按现有重新绑定流程处理；初始化不能绕过归属检查。

## 输出位置

默认结构：

```text
引擎工程/
  .gitignore                         # 加入 /gamecreator/personal/
  gamecreator/
    README.md                        # 完整工作流与目标 GameCreator 路径
    GAMECREATOR_GUIDE.md              # 指向管理项目的编写入口
    project.json                     # 项目身份、公开上下文与 authoring 路径
    personal/
      README.md                      # 成员名称与凭证文件对应关系
      <成员ID>.json                   # 该成员的完整私有凭证
      .gitignore                     # 忽略此目录内容
      .gdignore                      # Godot 工程中排除私有凭证资源
    context/                         # 开发反馈所用公开上下文
    feedback/                        # 引擎开发反馈
    receipts/                        # 反馈处理回执
  docs/gamecreator/
    README.md                        # 工作流、目录位置与分类索引
    modules/
      overview.md
      project-guide/                 # 项目规范、协作与写入入口
      project-management/            # 工程连接、排期、人员
      gameplay/                      # 玩法、关卡、原型、数值分析
      development/                   # 功能系统、开发工具、程序框架
      content/                       # 素材、故事与编排
      data-engine/                   # 数据、枚举、配置规范
```

协作入口也可以设置为例如 `team`。此时入口 README 和 personal 位于 `team/`，README 链接到固定的 `gamecreator/README.md`。后续普通工程同步会继续维护此入口，文档目录变化后同步更新链接。开发反馈仍使用 `gamecreator/feedback`，不迁移反馈协议目录。

初始化同时更新**绑定的 GameCreator 项目**中的 `GAMECREATOR_GUIDE.md`、`ai/README.md`、编写上下文、模板和校验提交脚本。内容编写工具仍属于这个管理项目，没有复制到引擎目录运行。

## 日常工作循环

1. AI 从引擎中的入口 README 开始，核对项目 ID、引擎目录和 GameCreator 项目目录，读取规范与当前文档。
2. 设计需要新增、重组或修改多个模块时，进入 README 指定的 GameCreator 项目目录，阅读 `GAMECREATOR_GUIDE.md` 和 `ai/README.md`。刷新协作文件，基于当前快照编写内容批次，校验并使用自己的凭证签名提交到 `ai/changes`。
3. 管理者在“使用说明 → 项目编写”读取并检查变更、处理冲突后应用。AI 查看该项目 `ai/receipts` 确认结果。
4. 将通过检查的设计文档与协作上下文同步回引擎；正式配置文件另走“数据同步”。开发者在引擎中实现和测试。
5. 进度、验收结论及现有需求建议写入引擎 `gamecreator/feedback`，在“工程同步 → 开发反馈”处理；后续再同步新的进度和上下文。

两套提交位置、快照与格式不可混用。直接编辑导出的 Markdown 不会回写 GameCreator。实际引擎效果为设计判断提供依据，代码当前行为不自动成为已验收要求。

## 重复执行与保护

- 重复初始化沿用已有身份和令牌，内容相同的凭证不重写。新增成员或更换令牌后可再次初始化。
- 私有凭证不进入公共文档、同步预览、同步历史或备份。初始化在写入凭证前添加 Git 忽略规则；已有自定义规则保留。目录本身不提供开发者之间的文件访问隔离，每位 AI 只使用分配给自己的凭证。
- 与已登记凭证不一致的外部文件会阻止写入；未修改的旧受管凭证可以更新。停用或删除成员、变更入口目录后，旧导出文件保留，权限校验仍以客户端当前授权和撤销状态为准。
- 普通文档沿用工程同步的冲突处理、备份、过期预览与中断恢复。旧平铺文档会列为待移除项，未经明确选择不会删除。
- 只有全部步骤成功才保存初始化完成记录。若文档已同步而后续凭证写入失败，页面报告失败，修复原因后重新预览初始化；不会重新签发令牌或把部分完成标为成功。
- 目录搬迁后重新保存工程连接并同步文档，更新 README 中的具体位置。

## 验证

`node --test tests/project-startup.test.cjs` 覆盖初始化、重复执行、自定义入口、令牌与文件冲突、归属和旧文档迁移。构建后运行 `node tests/desktop-project-startup.cjs` 验证客户端入口、准备状态、初始化、重启与路径显示。测试只使用独立临时项目。

关联说明：[工程内容同步](engine-content-sync.md)、[项目内容编写](project-authoring.md)、[开发反馈](engine-development-feedback.md)。
